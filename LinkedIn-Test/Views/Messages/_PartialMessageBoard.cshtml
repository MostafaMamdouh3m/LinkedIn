@model List<Message>

<div class="row" id="message_board_messages">

    @if (Model != null)
    {
        for (int i = Model.Count - 1; i >= 0; i--)
        {
            @Html.Partial("_PartialMessage", Model[i])
        }
    }

</div>
